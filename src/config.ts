// config.ts — external links, in one place.
//
// These appear in more than one part of the interface, so they live here rather
// than being typed out twice and drifting apart later.

/** Where the "buy me a coffee" button and footer link point. */
export const SUPPORT_URL = 'https://buymeacoffee.com/dinozoa';

/** Public source repository. */
export const SOURCE_URL = 'https://github.com/Skittzy/dinozoa';
